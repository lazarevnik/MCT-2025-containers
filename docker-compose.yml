services:
  db:
    image: postgres:13
    environment:
      POSTGRES_PASSWORD: "123"
      POSTGRES_USER: "user"
      POSTGRES_DB: "app"
    command: postgres
 
  app:
    build: .
    ports:
      - "5000:5000"
   
