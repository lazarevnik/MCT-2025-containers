version: "3.9"

services:
  db:
    image: postgres:15
    container_name: db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: app
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "psql -U postgres -d postgres -tAc \"SELECT 1 FROM pg_roles WHERE rolname='user'\" | grep -q 1"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7
    container_name: redis
    restart: always

  init_db:
    build:
      context: .
      dockerfile: ./app/init_db.Dockerfile
    container_name: init_db
    environment:
      - DB_HOST=db
      - DB_PORT=5432
      - DB_USER=user
      - DB_PASSWORD=password
      - DB_NAME=app
    depends_on:
      db:
        condition: service_healthy
    restart: "no"

  app:
    build:
      context: ./app
    container_name: app
    ports:
      - "5000:8000"
    environment:
      - DB_HOST=db
      - DB_PORT=5432
      - DB_USER=user
      - DB_PASSWORD=password
      - DB_NAME=app
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      init_db:
        condition: service_completed_successfully
      redis:
        condition: service_started

volumes:
  db_data: